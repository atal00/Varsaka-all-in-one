# Frontend (Vite) — base URL of the Varsaka API server.
# Production: the API host. Local dev: your running server/ instance.
VITE_API_BASE=https://api.varsaka.com
# VITE_API_BASE=http://localhost:4000
